<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://hopol.cc.cd</id>
    <title>流放之地</title>
    <updated>2020-02-28T21:03:05.138Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://hopol.cc.cd"/>
    <link rel="self" href="https://hopol.cc.cd/atom.xml"/>
    <subtitle>我像一顆棋子，被上帝抓在手中，我看到她的眼，卻聽不到她的心。</subtitle>
    <logo>https://hopol.cc.cd/images/avatar.png</logo>
    <icon>https://hopol.cc.cd/favicon.ico</icon>
    <rights>All rights reserved 2020, 流放之地</rights>
    <entry>
        <title type="html"><![CDATA[openwrt使用trojan教程（二）：在openwrt实现trojan透明代理]]></title>
        <id>https://hopol.cc.cd/post/wa6DgDa89/</id>
        <link href="https://hopol.cc.cd/post/wa6DgDa89/">
        </link>
        <updated>2020-02-28T21:00:06.000Z</updated>
        <content type="html"><![CDATA[<p>openwrt上使用trojan，我们继续探索，上一篇教程在<a href="https://www.atrandys.com/2020/2324.html">openwrt上编译trojan</a>，已经可以让trojan以socks5的方式跑起来，但是我们必须在应用中指定socks5代理，总的来说不够舒服，所以透明代理应该是我们的追求。</p>
<h2 id="openwrt-trojan透明代理方案">openwrt trojan透明代理方案</h2>
<h4 id="dnsmasq-full-ipset-iptables-dns-forwarder-redsocks-trojan">dnsmasq-full + ipset + iptables + dns-forwarder + redsocks + trojan</h4>
<p>透明代理绕不过的就是DNS污染问题，我们需要解决DNS污染，然后解决访问流量的代理。</p>
<p><strong>DNS</strong></p>
<blockquote>
<p>这里我们用直接使用dnsmasq指定gfwlist名单中的域名使用DNS服务器8.8.8.8，然后使用dns-forwarder将DNS的UDP转为TCP，再强制访问8.8.8.8的流量被代理。</p>
</blockquote>
<p><strong>代理</strong></p>
<blockquote>
<p>我们使用dnsmasq-full和ipset，让指定的域名的ip被记录和标记，然后通过iptables将其转发到redsocks转换的trojan代理，其他ip走默认路线。</p>
</blockquote>
<h2 id="opwnert下trojan透明代理教程">opwnert下trojan透明代理教程</h2>
<h3 id="1-安装trojan开启转发">1、安装trojan开启转发</h3>
<p>上一篇教程中我们编译了openwrt上的trojan ipk安装包，你若还没有编译，请参考这篇教程。</p>
<p><a href="https://www.atrandys.com/2020/2324.html"><strong>编译及安装教程</strong>：openwrt上编译trojan ipk并安装</a></p>
<p>开启转发</p>
<pre><code>echo “net.ipv4.ip_forward=1” &gt;&gt; /etc/sysctl.conf

sysctl -p
</code></pre>
<p>**PS：**这里一定注意修改/etc/config/trojan配置文件，将enabled改为1。</p>
<h3 id="2-安装dnsmasq-full-ipset-dns-forwarder-redsocks">2、安装dnsmasq-full ipset dns-forwarder redsocks</h3>
<p><strong>安装dnsmasq-full ipset redsocks</strong></p>
<pre><code>opkg update
opkg remove dnsmasq &amp;&amp; opkg install dnsmasq-full
opkg install ipset iptables-mod-nat-extra redsocks
</code></pre>
<p><strong>安装dns-forwarder</strong></p>
<p>首先我们看一下你的openwrt CPU架构</p>
<pre><code>opkg print-architecture | awk '{print $2}' | grep -v all | grep -v noarch
</code></pre>
<p>然后去dns-forwarder网站下载对应架构的ipk：<a href="https://dl.bintray.com/aa65535/openwrt/dns-forwarder/1.2.1-2/current/">去下载dns-forwarder</a></p>
<p>将dns-forwarder上传到openwrt或者直接wget下载，然后安装dns-forwarder</p>
<pre><code>opkg install dns-forwarder*
</code></pre>
<h3 id="3-配置trojan">3、配置trojan</h3>
<p>我们之前的教程中使用nohup运行trojan，今天我们使用配置trojan自启动，首先我们在trojan server端搭建时获取两个文件。</p>
<blockquote>
<p>/usr/src/trojan-cli/config.json<br>
/usr/src/trojan-cli/fullchain.cer</p>
</blockquote>
<p>将这两个文件下载并重新命名config.json为trojan.json，然后将修改cert的值为/etc/fullchain.cer</p>
<figure data-type="image" tabindex="1"><img src="https://hopol.cc.cd/post-images/1582923753763.png" alt="" loading="lazy"></figure>
<p>然后将trojan.json和fullchain.cer上传到openwrt的/etc/目录下即可。</p>
<p>开启trojan自启动</p>
<pre><code>/etc/init.d/trojan enable
</code></pre>
<p>运行trojan</p>
<pre><code>/etc/init.d/trojan start
</code></pre>
<h3 id="4-配置dns-forwarder">4、配置dns-forwarder</h3>
<p>编辑dns-forwarder配置文件</p>
<pre><code>vi /etc/config/dns-forwarder
</code></pre>
<p>修改内容如下，然后保存</p>
<pre><code>config dns-forwarder
option listen_addr '0.0.0.0'
option listen_port '5353'
option dns_servers '8.8.8.8'
option enable '1'
</code></pre>
<p>重启dns-forwarder</p>
<pre><code>/etc/init.d/dns-forwarder restart
</code></pre>
<h3 id="5-配置redsocks">5、配置redsocks</h3>
<pre><code>cat &gt; /etc/redsocks.conf &lt;&lt;-EOF

base {
log_debug = off;
log_info = on;
log = &quot;syslog:local7&quot;;
daemon = on;
redirector = iptables;
}

redsocks {
local_ip = 0.0.0.0;
local_port = 12345;
ip = 127.0.0.1;
port = 1080;
type = socks5;

}

EOF
</code></pre>
<p>然后重启redsocks</p>
<pre><code>/etc/init.d/redsocks restart
</code></pre>
<h3 id="6-配置dnsmasq-ipset-iptables">6、配置dnsmasq、ipset、iptables</h3>
<p>首先为dnsmasq的配置文件添加conf-dir</p>
<pre><code>echo &quot;conf-dir=/etc/dnsmasq.d&quot; &gt;&gt; /etc/dnsmasq.conf
</code></pre>
<p>新建刚才添加的文件夹并进入</p>
<pre><code>mkdir /etc/dnsmasq.d/ &amp;&amp; cd /etc/dnsmasq.d/
</code></pre>
<p>将配置好的gfwlist下载下来（注意，我把本站加入了代理，其他网站来源于github：gfwlist）</p>
<pre><code>wget https://raw.githubusercontent.com/atrandys/gfwlist/master/gfwlist.conf
</code></pre>
<p>新建一个ipset gfwlist表</p>
<pre><code>ipset -N gfwlist iphash
</code></pre>
<p>配置iptables将NAT转发到redsocks</p>
<pre><code>iptables -t nat -A PREROUTING -p tcp -m set --match-set gfwlist dst -j REDIRECT --to-ports 12345
</code></pre>
<p>配置iptables将openwrt本机转发到redsocks</p>
<pre><code>iptables -t nat -A OUTPUT -p tcp -m set --match-set gfwlist dst -j REDIRECT --to-ports 12345
</code></pre>
<p>配置TCP访问8.8.8.8的DNS解析流量走代理</p>
<pre><code>iptables -t nat -A PREROUTING -p tcp -d 8.8.8.8/32 -j REDIRECT --to-ports 12345
iptables -t nat -A OUTPUT -p tcp -d 8.8.8.8/32 -j REDIRECT --to-ports 12345
</code></pre>
<p>然后重启dnsmasq，用浏览器访问一下google，看一下透明代理是否生效吧。</p>
<pre><code>/etc/init.d/dnsmasq restart 若需要配置以上规则开机自动添加，将以上ipset和iptables开头的5条命令逐行加入到/etc/firewall.user文件内容的末尾即可。
</code></pre>
<p>原帖：https://www.atrandys.com/2020/2343.html</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[openwrt使用trojan教程（一）：openwrt编译trojan ipk，支持tls1.3，使用openwrt sdk，速度快]]></title>
        <id>https://hopol.cc.cd/post/fNkcW0Erf/</id>
        <link href="https://hopol.cc.cd/post/fNkcW0Erf/">
        </link>
        <updated>2020-02-28T20:41:23.000Z</updated>
        <content type="html"><![CDATA[<p>为openwrt编译trojan，在openwrt使用trojan，有很多大佬编译好的固件支持，您可以去直接用，但这里我只是想在纯净openwrt上使用trojan，不需要支持其他的代理，所以出一个openwrt上使用trojan的教程。</p>
<p>这个教程会有几篇文章，大概包含以下内容</p>
<blockquote>
<p>openwrt使用trojan教程（一）：编译支持你的openwrt路由器CPU架构的trojan安装包</p>
<p>openwrt使用trojan教程（二）：在openwrt实现trojan透明代理，编译相关luci-app支持界面管理</p>
<p>openwrt使用trojan教程（？）</p>
</blockquote>
<p>今天我们讲怎么编译openwrt上的trojan ipk包，编译真的是不断解决各种缺失依赖的过程，为了尽可能成功，接下来请使用我教程一致的编译环境，我使用ubuntu18.04来编译trojan，不要用VPS服务商提供的minimal版，因为需要从openwrt和用git下载很多包，网络环境用外网VPS最好，国内速度很慢，或者国内用速度不错的VPN全局代理也可以。</p>
<h2 id="openwrt编译trojan教程">openwrt编译trojan教程</h2>
<h3 id="一-下载你路由器cpu架构对应的openwrt-sdk">一、下载你路由器CPU架构对应的openwrt SDK</h3>
<p>1、下载CPU架构对应的openwrt SDK</p>
<p>查看你的openwrt信息，比如我在翻车讯K2P上用，CPU为MT7621</p>
<p>进入openwrt官网，下载SDK，点击访问： https://downloads.openwrt.org/releases/</p>
<p>找到你的openwrt对应版本，我这里用最新的19.07.1，进入target，进入选择ramips，然后选择MT7621，拉到最下方下载openwrt-sdk开头的文件，复制这个链接的下载地址</p>
<figure data-type="image" tabindex="1"><img src="https://hopol.cc.cd/post-images/1582922703090.png" alt="" loading="lazy"></figure>
<pre><code>wget https://downloads.openwrt.org/releases/19.07.1/targets/ramips/mt7621/openwrt-sdk-19.07.1-ramips-mt7621_gcc-7.5.0_musl.Linux-x86_64.tar.xz
</code></pre>
<p>然后解压缩</p>
<pre><code>tar xvf openwrt-sdk*
</code></pre>
<h3 id="二-安装编译环境">二、安装编译环境</h3>
<p>这个编译环境之前编译udp2raw和udpspeeder的时候用的，为了解决依赖搞了很久，现在我们直接拿来用。</p>
<pre><code>apt-get update
</code></pre>
<p>先更新，后安装必要的工具</p>
<pre><code>apt-get install subversion build-essential libncurses5-dev zlib1g-dev gawk git ccache gettext libssl-dev xsltproc wget unzip python time libcloog-isl-dev
</code></pre>
<h3 id="三-编译trojan">三、编译trojan</h3>
<p>openwrt-trojan github地址：https://github.com/trojan-gfw/openwrt-trojan</p>
<p>先进入openwrt sdk package目录</p>
<pre><code>cd openwrt-sdk*/package
</code></pre>
<p>下载github上的openwrt-trojan</p>
<pre><code>git clone https://github.com/trojan-gfw/openwrt-trojan.git
</code></pre>
<p>移动一下文件</p>
<pre><code>mv openwrt-trojan/* ./
</code></pre>
<p>回到openwrt-sdk目录</p>
<pre><code>cd ..
</code></pre>
<p>安装feeds</p>
<pre><code>./scripts/feeds update -a
./scripts/feeds install -a
</code></pre>
<p>进入make menuconfig</p>
<pre><code>make menuconfig
</code></pre>
<p>进入全局配置</p>
<p>勾掉前三个星号，保存</p>
<p>进入network</p>
<p>勾选trojan，保存，退出，然后执行编译trojan，如果你的编译环境有多核CPU，可以使用-j 4参数指定CPU使用4核心</p>
<pre><code>make package/trojan/compile -j 4 V=99
</code></pre>
<p>等待编译完成进入bin/packages/你的cpu架构/base下找到trojan开头的ipk，将其下载即可</p>
<p>四、安装trojan</p>
<p>将trojan ipk文件上传到openwrt</p>
<pre><code>opkg update
opkg install trojan-*
</code></pre>
<p>五、让trojan在openwrt上跑起socks代理<br>
首先我们先改一下trojan的默认配置文件，将文件中0改为1</p>
<pre><code>vi /etc/config/trojan
</code></pre>
<p>新建一个目录</p>
<pre><code>mkdir /etc/trojan
</code></pre>
<p>编辑配置文件，将客户端config.json添加进来，服务端搭建可以参考本站的<a href="https://www.atrandys.com/2019/1963.html">trojan一键脚本，支持debian9+/ubuntu16.04+/centos7+</a>，注意一键脚本配置好的config.json在/usr/src/trojan-cli/目录下，并注意将监听的地址由127.0.0.1改为0.0.0.0，将cert的值改为/etc/trojan/fullchain.cer，将fullchain.cer文件下载放到/etc/trojan/目录下。</p>
<pre><code>vi /etc/trojan/config.json
</code></pre>
<p>然后安装nohup工具，并运行trojan</p>
<pre><code>opkg install coreutils-nohup
cd /etc/trojan &amp;&amp; nohup trojan &gt;trojan.log 2&gt;&amp;1 &amp;
</code></pre>
<p>六、浏览器中配置socks5指向路由器IP:1080即可</p>
<p>这里我们是只配置了socks5代理，后续教程我们会努力实现openwrt的trojan透明代理。</p>
<p>转自：https://www.atrandys.com/2020/2324.html</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[trojan一键脚本，开启TLS1.3，系统支持centos7+ debian9+ ubuntu16+]]></title>
        <id>https://hopol.cc.cd/post/mbru27YzF/</id>
        <link href="https://hopol.cc.cd/post/mbru27YzF/">
        </link>
        <updated>2020-02-21T03:03:33.000Z</updated>
        <content type="html"><![CDATA[<p>最简单的Trojan三合一一键脚本，效率高/速度快/延迟低，系统支持centos7+/debian9+/ubuntu16+，完美支持tls1.3，速度优于V2ray+ws+tls1.3，新手必备。</p>
<h2 id="使用方法">使用方法</h2>
<p>1、执行以下脚本，然后选择1，安装trojan，然后输入解析到VPS的域名，回车安装即可。注意：安装时不要开启CDN，否则会提示IP地址不匹配。</p>
<pre><code>curl -O https://raw.githubusercontent.com/atrandys/trojan/master/trojan_mult.sh &amp;&amp; chmod +x trojan_mult.sh &amp;&amp; ./trojan_mult.sh
</code></pre>
<p>2、下载客户端、安装浏览器插件，启用trojan</p>
<p>这里的教程其实很简单，脚本安装完成会提示一个下载地址，复制到浏览器即可下载windows版客户端，然后需要配合浏览器插件使用。</p>
<h2 id="原帖地址">原帖地址：</h2>
<p>https://www.atrandys.com/2019/2154.html</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Brook iptables端口转发 一键管理脚本，支持DDNS]]></title>
        <id>https://hopol.cc.cd/post/djc6Wt_gU/</id>
        <link href="https://hopol.cc.cd/post/djc6Wt_gU/">
        </link>
        <updated>2020-02-21T02:58:56.000Z</updated>
        <content type="html"><![CDATA[<p><strong>一、Brook 脚本版</strong></p>
<p>基于原 Brook端口转发 一键管理脚本 的修改版，增加对动态域名的支持，开启监控后自动更新域名IP。</p>
<p>基于iptables防火墙，记得安装iptables。</p>
<p>安装Brook，然后配置端口转发，最后启动Brook。</p>
<pre><code>wget  -N --no-check-certificate https://zhujiwiki.com/wp-content/uploads/2020/01//brook-pf-mod.sh &amp;&amp; chmod +x brook-pf-mod.sh
./brook-pf-mod.sh

</code></pre>
<p>![Brook/iptables端口转发 一键管理脚本，支持DDNS]<br>
<img src="https://hopol.cc.cd/post-images/1582253999990.png" alt="" loading="lazy"><img src="images/20200220205730163_15333.png" alt="" loading="lazy"></p>
<p>![Brook/iptables端口转发 一键管理脚本，支持DDNS]<br>
<img src="https://hopol.cc.cd/post-images/1582254011469.png" alt="" loading="lazy"><img src="images/20200220205718349_29219.png" alt="" loading="lazy"></p>
<p><strong>二、iptables 脚本版</strong></p>
<pre><code>wget  -N --no-check-certificate https://zhujiwiki.com/wp-content/uploads/2020/01/iptables-pf.sh &amp;&amp; chmod +x iptables-pf.sh
bash iptables-pf.sh

</code></pre>
<p><strong>三、相关问题</strong></p>
<p>1、卸载 firewalld 和 ufw</p>
<pre><code>apt remove firewalld ufw

</code></pre>
<p>或</p>
<pre><code>yum remove firewalld ufw

</code></pre>
<p>然后重启。</p>
<p>2、安装iptables</p>
<pre><code>yum install -y iptables
yum update iptables 
yum install iptables-services

</code></pre>
<p>iptables配置文件：/etc/sysconfig/iptables</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[2020 年十大热门 BT 站]]></title>
        <id>https://hopol.cc.cd/post/qKl-WVeOx/</id>
        <link href="https://hopol.cc.cd/post/qKl-WVeOx/">
        </link>
        <updated>2020-02-21T02:56:43.000Z</updated>
        <content type="html"><![CDATA[<p>2020 年初访问量大的 BT 站有哪些？延续前几年的传统，今年的榜单中海盗湾再次成为第一，取代了去年的第一名 YTS。同样引人注目的是，这份榜单和去年相比没有比较大的变动，说明去年没有一个大型 BT 站被关停。</p>
<p>就人气而言，近年来，BT 站已经被流媒体网站超越。然而，它们仍然很受欢迎。</p>
<p>进入新的一年，我们看到海盗湾再次领先，从去年的第三名升至第一。在经历了一段动荡的时间后，尽管仍然不允许新用户注册，但海盗湾在过去的一年里相对稳定。</p>
<p>从更广泛的角度看，今年整个榜单相对完好无损。有几个网站排名发生变化，但没有一个主要网站消失，这本身就已经是一个相当大的成就了。</p>
<p>今年的榜单包括两个新上榜站点。以游戏为导向的 Fitgirl Repacks 排在第九位，印度网站 Tamil Rockers 排在第十位。这两家取代了 TorrentDownloads 和 Zooqle。</p>
<p>按照惯例，因为本文主要针对英语用户和面向国际受众的 BT 站进行排名，所以即使 <a href="https://rutracker.org/forum/index.php">RuTracker</a>,<a href="https://torrentwal2.com/">TorrentWal</a>,<a href="http://rutor.info/">Rutor</a>,<a href="https://dytt8.net/">Dytt8</a>,<a href="http://etoland.co.kr/">Etoland</a> 符合流量条件，也不会包括在内。</p>
<p>以下是新年伊始访问量最大的 BT 站的榜单，这个排行榜是基于 Alexa 的数据。</p>
<hr>
<h2 id="1-海盗湾">1、海盗湾</h2>
<p>网址：https://thepiratebay.org/</p>
<p>海盗湾建站至今超过 16 年，仍在继续运营。在经历了 2018 年的严重停机事故之后，去年表现相对平稳。该网站几个月前关闭了新用户注册，但仍向公众开放。它仍在使用 .org 域名，但不确定将来是否会如此。</p>
<h2 id="2-ytslt">2、YTS.lt</h2>
<p>网址: https://yts.lt/</p>
<p>这个网站是原 YTS/YIFY 的非官方继任者。该网站侧重于全球热门的电影发布。YTS 最近成为美国三起诉讼的被告，虽然运营商签署同意了判决，向一名电影制作人支付损害赔偿金，但该网站仍处于在线状态。</p>
<h2 id="3-1337x">3、1337X</h2>
<p>网址：https://1337x.to/</p>
<p>1337X 保住了前三的位子，与其他一些网站不同的是，它有一群忠实的 up 主，每天都会提供新种子。</p>
<h2 id="4-rarbg">4、RARBG</h2>
<p>网址：https://rarbg.to/</p>
<p>在过去的一年里，RARBG 一直保持稳定运营。RARBG 成立于 2008 年，该站在高清电影和电视剧方面更新速度较快。该站有多个域名，这里选取访问量最大的域名进行排名。</p>
<h2 id="5-nyaasi">5、NYAA.si</h2>
<p>网址：https://nyaa.si/</p>
<p>NYAA.si 是知名动漫 BT 站 NYAA 关站重生后的网站。虽然有来自其他 BT 站的激烈竞争，但 NYAA 表现良好，与去年排名相比上升了一位。</p>
<h2 id="6-torrentz2">6、Torrentz2</h2>
<p>网址：https://torrentz2.eu/</p>
<p>Torrentz2 是 Torrentz.eu 的替代网站，Torrentz.eu 网站于 2016 年自愿关闭。该网站没有存储任何种子文件，但仍然是一个很受欢迎的种子搜索引擎。</p>
<h2 id="7-eztvio">7、EZTV.io</h2>
<p>网址：https://eztv.io/</p>
<p>最初的电视剧发布组 EZTV 在 2015 年被恶意收购后关站，新所有者声称拥有该品牌。该组织去年改用了一个新域名，由于与海盗湾（Pirate Bay）共用了 IP 地址，因此无意中被荷兰封锁屏蔽。</p>
<h2 id="8-limetorrents">8、LimeTorrents</h2>
<p>网址：https://www.limetorrents.info/</p>
<p>LimeTorrents 已经有十多年的历史了。跟这个榜单中的其他网站一样，被世界各地的 ISP 封锁，在一定程度上影响了整体访问流量。</p>
<h2 id="9-fitgirl-repacks">9、Fitgirl Repacks</h2>
<p>网址：https://fitgirl-repacks.site/</p>
<p>FitGirl Repacks 绝不是一个传统的 BT 站。它是一个游戏发布组的网站，它发布热门游戏的精简破解版，从而将下载时间降至最低。他们在其他网站上发布种子，但也提供他们自己的磁力链接，这就是为什么我们在这里加入这个网站的原因。</p>
<h2 id="10-tamil-rockers">10、Tamil Rockers</h2>
<p>网址：https://tamilrockers.ws/</p>
<p>TamilRockers 在印度非常出名，并设有基地。由于它有英文版本，该网站在全球范围内都有使用。尽管印度发起过几次执法行动，并逮捕了涉嫌的管理员和操作员，但该网站仍在蓬勃发展。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[trojan多用户配置，简单模式+使用trojan_mysql高级模式，流量管理]]></title>
        <id>https://hopol.cc.cd/post/tFnhE2ogD/</id>
        <link href="https://hopol.cc.cd/post/tFnhE2ogD/">
        </link>
        <updated>2020-02-21T02:51:34.000Z</updated>
        <content type="html"><![CDATA[<p>trojan多用户的配置方法，有很多小伙伴问过，这里写一篇trojan多用户配置的教程，包含两种方法，一种是最简单的配置文件的多用户和一种使用到mysql数据库的多用户，这两种方法在trojan的官方文档里都有介绍，我来翻译一下白话文吧。</p>
<p>两种方法说明</p>
<blockquote>
<p>1、配置文件多用户，只需在配置文件中多添加几个密码即可</p>
<p>2、使用mysql的多用户，需要安装mysql，支持每个用户的流量管理</p>
</blockquote>
<h2 id="使用配置文件配置trojan多用户方法">使用配置文件配置trojan多用户方法</h2>
<p>1、找到trojan的服务端配置文件server.conf，如果是使用本站一键脚本安装的，那么位置在/usr/src/trojan/server.conf</p>
<p>2、修改server.conf，在password数组中添加多个值，如下图所示，一定要加引号，多个密码间注意使用英文逗号隔开，最后一个密码后无需加逗号</p>
<figure data-type="image" tabindex="1"><img src="https://hopol.cc.cd/post-images/1582253571731.png" alt="" loading="lazy"></figure>
<p>添加完成，保存文件，然后重启trojan或VPS</p>
<p>重启trojan：systemctl restart trojan</p>
<p>重启VPS：reboot</p>
<h2 id="使用mysql配置trojan多用户方法">使用mysql配置trojan多用户方法</h2>
<p>这种方法稍微复杂一些，其实这里可以开发出一个panel管理面板，大火酱好像有开发计划，这里咱们讲如何手动配置吧，以centos7为例。</p>
<p>1、安装mysql，配置mysql用户名密码，新建trojan的user表</p>
<p>mysql这里我们安装mysql5.*版本，比较好管理</p>
<pre><code>wget http://repo.mysql.com/mysql-community-release-el7-5.noarch.rpm
rpm -ivh mysql-community-release-el7-5.noarch.rpm
yum -y install mysql-server
systemctl enable mysqld.service
systemctl start mysqld.service
</code></pre>
<p>然后配置mysql，创建数据库</p>
<pre><code>mysql_secure_installation

回车

Y 回车

输入新的root密码 回车

再次输入root密码 回车

Y 回车

Y 回车

Y 回车

Y 回车
</code></pre>
<p>一路如上图操作，然后创建数据库</p>
<pre><code>mysql -u root -p

输入上一步设置的密码

create database trojan;

use trojan;

创建users表

CREATE TABLE users (
id INT UNSIGNED NOT NULL AUTO_INCREMENT,
username VARCHAR(64) NOT NULL,
password CHAR(56) NOT NULL,
quota BIGINT NOT NULL DEFAULT 0,
download BIGINT UNSIGNED NOT NULL DEFAULT 0,
upload BIGINT UNSIGNED NOT NULL DEFAULT 0,
PRIMARY KEY (id),
INDEX (password)
);
exit;
</code></pre>
<p>2、修改trojan server配置文件，支持mysql并配置第一步的用户名密码</p>
<p>trojan的server文件，如果是本站一键脚本，一般在/usr/src/trojan/server.conf下。修改如下图中的配置，将enabled改为true，将username改为root，password改为你设置的数据库密码</p>
<figure data-type="image" tabindex="2"><img src="https://hopol.cc.cd/post-images/1582253597313.png" alt="" loading="lazy"></figure>
<p>3、user表中增加用户名、密码、可用流量</p>
<p>这一步我们需要在用户users表中插入用户信息，trojan会首先寻找server</p>
<p>密码：需要sha224加密才可以，下面命令可以生成sha224加密后的密码</p>
<pre><code>echo -n &quot;你的密码&quot; | sha224sum | awk '{print $1}'
</code></pre>
<p>可用流量：如果不限制就写一个负数，例如-100，如果需要限制，就填写一个数字，单位是B字节，所以如果是10G流量，那么就是10<em>1024</em>1024*1024，自己计算就可以了。</p>
<p>然后连接mysql，然后insert into users，执行下面命令</p>
<pre><code>mysql -u root -p

输入mysql密码

use trojan;

insert into users (username, password, quota) values (&quot;user1&quot;, &quot;这里是sha224加密后的密码&quot;, 12342312312);

exit;
</code></pre>
<p>然后测试能否正常访问即可；</p>
<h2 id="trojan多用户问题记录">trojan多用户问题记录</h2>
<p>trojan多用户的高级用法已经测试成功，但目前的问题是，如果trojan一直不断开，流量即使用超了也可以一直连接，只有等到下一次重新连接的时候才会限制生效。同样的道理，如果当前用户用完了流量，你在服务端增加了流量之后，用户也必须重新启动一下trojan才能生效。</p>
<h2 id="原帖地址">原帖地址：</h2>
<p>https://www.atrandys.com/2020/2269.html</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[[备份]稳上加稳，trojan+haproxy本地负载均衡连接多条trojan服务端，高可用or分散流量多种模式]]></title>
        <id>https://hopol.cc.cd/post/m7PuuiW1B/</id>
        <link href="https://hopol.cc.cd/post/m7PuuiW1B/">
        </link>
        <updated>2020-02-21T01:56:20.000Z</updated>
        <content type="html"><![CDATA[<p>实验性方案，没有折腾精神的小伙伴看个热闹就好了。</p>
<p>最近tg群里有同学谈到在openwrt中使用trojan，他讲到了一款叫做passwall的软件，看名称你就能知道它是做什么的，它是支持trojan的，同时也支持负载均衡，它的负载均衡厉害的地方在于你后端可以同时用任何代理，例如你添加了多条路线，包含ss/v2ray/trojan等，passwall可以把流量很好的转发到这些代理端，其实这也是路由器科学上网的好处。因为trojan官方已经有了openwrt版，所以最近也在想做一个trojan的openwrt luci插件，方便管理，这看看有时间做吧。</p>
<p>这也让我想到了之前玩过的trojan+haproxy在windows PC中负载均衡的方式，简单说就是本地使用trojan客户端的流量通过haproxy负载均衡到后端的多个trojan服务端。这里就记录一下过程。</p>
<h2 id="trojan负载均衡说明">trojan负载均衡说明</h2>
<blockquote>
<p>1、trojan的负载均衡主要靠配置haproxy来实现，haproxy有多种算法，我们需要里找到不同场景下最合适的算法，所以不同需求场景会有不同的配置。</p>
<p>2、trojan的服务端需要有相同的密码配置。</p>
</blockquote>
<h2 id="trojan负载均衡服务端搭建一键脚本">trojan负载均衡服务端搭建一键脚本</h2>
<p><strong>1、这个脚本在之前的基础上稍微改了一下，需要手动输入参数，支持centos7+/ubuntu16.04+/debian9+</strong></p>
<pre><code>wget https://raw.githubusercontent.com/atrandys/trojan/master/trojan4balance.sh &amp;&amp; chmod +x trojan4balance.sh
</code></pre>
<p><strong>2、然后执行脚本，此时需要输入两个参数，第一个是你绑定到VPS的域名（举例：s1.atrandys.com），第二个是密码（举例：atrandys），这个密码所有负载均衡的server端都必须一样。</strong></p>
<pre><code>./trojan4balance.sh s1.atrandys.com atrandys
</code></pre>
<p><strong>3、搭建完成检查是否成功，使用这个命令看到nginx进程和trojan进程就基本确认安装成功。</strong></p>
<pre><code>ps -aux | grep -e trojan -e nginx | grep -v grep
</code></pre>
<h2 id="trojan负载均衡客户端下载">trojan负载均衡客户端下载</h2>
<p><strong>1、trojan的客户端已经包含了负载均衡软件haproxy（1.7windows版，集成cygwin），你只需要下载下来即可</strong></p>
<p>下载地址<br>
https://github.com/atrandys/trojan/raw/master/trojan-cli-haproxy.zip</p>
<p><strong>2、编辑客户端trojan的配置文件config.json和haproxy的配置文件haproxy.conf</strong></p>
<p>修改config.jason，将password中的atrandys修改为你设置的密码</p>
<figure data-type="image" tabindex="1"><img src="https://hopol.cc.cd/post-images/1582250681379.png" alt="" loading="lazy"></figure>
<p>修改haproxy.conf，将s1.atrandys.com/s2.atrandys.com/s3.atrandys.com分别修改成你后端trojan服务器的域名，我这里是三个，如果你的数量不同自行增减行数即可。</p>
<figure data-type="image" tabindex="2"><img src="https://hopol.cc.cd/post-images/1582250694977.png" alt="" loading="lazy"></figure>
<h2 id="haproxy负载均衡算法与适合的场景">haproxy负载均衡算法与适合的场景</h2>
<p><strong>1、统计与健康检测</strong></p>
<p><strong>统计</strong></p>
<p>你可以在本地浏览器中访问<a href="http://127.0.0.1:2080/">127.0.0.1:2080</a>，进入haproxy的统计页面，查看统计信息。</p>
<p><strong>tcp模式健康检测</strong></p>
<p>tcp模式的haproxy健康检测通过tcp握手检测，配置为check即可。</p>
<p><strong>http模式健康检测</strong></p>
<p>这个我们没用到，用了最简单的tcp模式，谷歌关键词“haproxy http健康检测”</p>
<p>2、负载均衡算法</p>
<p>roundrobin：简单轮询，按照权重分配，适合流量分散到不同后端，后端server访问慢的情况有点问题，待研究。</p>
<p>source：同一访问地址固定访问同一台服务器，本机IP算是固定的，所以只会访问一台服务器，当服务器不可用时才会切换其他。</p>
<p>待补充……</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[[备份]ss v2ray等+kcptun+udp2raw，垃圾线路拯救者，避免高峰期QOS，秒开4K]]></title>
        <id>https://hopol.cc.cd/post/Xzfu6wkgF/</id>
        <link href="https://hopol.cc.cd/post/Xzfu6wkgF/">
        </link>
        <updated>2020-02-21T01:42:25.000Z</updated>
        <content type="html"><![CDATA[<p>最近很多朋友说高峰期太卡了，其实没办法，人人憋在家里，外网访问流量自然比平时大，这时候国际出口带宽有限，就导致原本不是高峰期的时段也变成高峰期了，所以这个时候CN2 GIA线路和IPLC线路都特别火，这属于加钱享受更好的服务了，无可厚非。但对于普通用户来说，运营商是区别对待的，这就有点不公平了，我是163骨干网线路，你是GIA线路，同样高峰期，你丢包0，我丢包30%，归根到底是运营商没有那么大的带宽，承载不了国内基数如此大的网民上网需求，这就像没有金刚钻不揽瓷器活儿，你能力不足还给我QOS，那么作为用户想办法抢占一下带宽也是合情合理的。只不过对于没有使用kcptun多倍发包的用户，是不公平的，就像高速公路运输货物，你可以有多辆卡车一起运送，而其他用户只能用1辆卡车运送。话说回来，信息不对称造成的不公平处处存在，此处也就不需要矫情太多了。</p>
<h2 id="说明">说明</h2>
<blockquote>
<p>1、只做代理用途，所以不要问怎么配代理游戏了，我们只管TCP</p>
<p>2、这种方式应对QOS是有效的，但如果运营商无耻到封本地端口，那无解，只能换端口或则重启路由器/光猫</p>
<p>3、理论上SS、v2ray等TCP代理都是可以搭配 KCPTUN和udp2raw使用</p>
<p>4、教程手动配置，比较繁琐但不难，建议耐心细心，交流tg group: https://t.me/atrandys</p>
<p>5、免责声明：此种方式适合有折腾精神的同学，相比trojan等或许会有相对高一点的被墙风险（有点玄学）。</p>
</blockquote>
<h2 id="搭建教程">搭建教程</h2>
<h3 id="1-安装代理软件">1、安装代理软件</h3>
<p>这里不多说了，SS或者V2ray或者其他使用TCP协议的代理都可以，自行安装，我这里使用SS</p>
<p>下面的一键脚本是自己改过的，少了一些可配置的东西，一键到底，你使用其他的搭建方式也可以。</p>
<pre><code>wget https://raw.githubusercontent.com/atrandys/ssr/master/ss_sin.sh &amp;&amp; chmod +x ss_sin.sh &amp;&amp; ./ss_sin.sh

</code></pre>
<p>搭建完成后，我们需要记录下来SS服务端监听的端口，例如为37212（后面配置要用），测试SS或其他代理软件正常使用，这一步测试很重要，确保无误，方便万一不能正常使用的情况我们排查问题。</p>
<h3 id="2-服务端安装kcptun和udp2raw并配置">2、服务端安装kcptun和udp2raw并配置</h3>
<p>两个软件在github的下载地址，贴在下面，可以自己去下载（如果你搞不清楚，直接用下面步骤的命令即可），后面我会把解压的最新版放在github，写在命令里 ，你可以直接用，无需自己下载解压。</p>
<p><strong>kcptun</strong></p>
<blockquote>
<p>server：https://github.com/xtaci/kcptun/releases/download/v20200201/kcptun-linux-amd64-20200201.tar.gz</p>
<p>client（win64）：https://github.com/xtaci/kcptun/releases/download/v20200201/kcptun-windows-amd64-20200201.tar.gz</p>
</blockquote>
<p><strong>udp2raw</strong></p>
<blockquote>
<p>server（amd64）：https://github.com/wangyu-/udp2raw-tunnel/releases/download/20190716.test.0/udp2raw_binaries.tar.gz</p>
<p>client（win64）：https://github.com/wangyu-/udp2raw-multiplatform/releases/download/20190407.0/udp2raw_windows.zip</p>
</blockquote>
<p>2.1 配置udp2raw</p>
<p>因为整个方案udp2raw是在最外层的，所以在服务端我们需要暴露udp2raw监听的端口，如果你启用了防火墙或者服务商自带防火墙，需要开放这个端口，这里我使用8080为udp2raw监听的端口，而udp2raw要指向kcptun的端口，我们这里使用9090（这些端口你要记好，后面会用到）。</p>
<p>首先我们新建一个文件夹，用来存放我们使用的软件</p>
<pre><code>mkdir /usr/src/kcpudp &amp;&amp; cd /usr/src/kcpudp

</code></pre>
<p>服务端下载udp2raw</p>
<pre><code>wget https://github.com/atrandys/ssr/raw/master/src/udp2raw_amd64 &amp;&amp; wget https://raw.githubusercontent.com/atrandys/ssr/master/src/run.sh &amp;&amp; chmod +x udp2raw_amd64 run.sh

</code></pre>
<p>启用udp2raw，注意8080是udp2raw监听的端口，指向9090是kcptun监听的端口，“atrandys”你可以任意替换成其他内容，客户端和这个数值一致就可以（后面客户端配置你会看到）。</p>
<pre><code>nohup ./run.sh ./udp2raw_amd64 -s -l0.0.0.0:8080 -r 127.0.0.1:9090 --raw-mode faketcp --cipher-mode none -a -k &quot;atrandys&quot; &gt;udp2raw.log 2&gt;&amp;1 &amp;

</code></pre>
<p>2.2 配置kcptun</p>
<p>首先下载kcptun</p>
<pre><code>wget https://github.com/atrandys/ssr/raw/master/src/server_linux_amd64 &amp;&amp; chmod +x server_linux_amd64

</code></pre>
<p>然后写配置文件，注意listen监听9090，这kcptun监听的端口，target为127.0.0.1:37212，这里指向的是SS的端口。</p>
<blockquote>
<pre><code>cat &gt; kcptun_server.json&lt;&lt;-EOF
{
&quot;listen&quot;: &quot;:9090&quot;,
&quot;target&quot;: &quot;127.0.0.1:37212&quot;,
&quot;key&quot;: &quot;atrandys&quot;,
&quot;crypt&quot;: &quot;&quot;,
&quot;mode&quot;: &quot;fast2&quot;,
&quot;mtu&quot;: 1200,
&quot;sndwnd&quot;: 1024,
&quot;rcvwnd&quot;: 512,
&quot;datashard&quot;: 5,
&quot;parityshard&quot;: 1,
&quot;dscp&quot;: 0,
&quot;nocomp&quot;: true
}
EOF

</code></pre>
</blockquote>
<p>然后运行kcptun服务端</p>
<pre><code>nohup ./server_linux_amd64 -c ./kcptun_server.json &gt;kcptun.log 2&gt;&amp;1 &amp;

</code></pre>
<h3 id="3-客户端安装kcptun和udp2raw并配置ss客户端">3、客户端安装kcptun和udp2raw并配置SS客户端</h3>
<p>1、下载客户端软件kcptun和udp2raw（win64）</p>
<p>注意使用udp2raw需要你的电脑安装winpcap或者npcap</p>
<p>下载地址：https://nmap.org/npcap/dist/npcap-0.9987.exe</p>
<p>下载客户端，客户端存放了配置文件和软件，下载下来解压得到kcpudp文件夹</p>
<p>下载地址：https://github.com/atrandys/ssr/raw/master/src/kcpudp.zip</p>
<p>2、修改文件夹内参数</p>
<p>修改start.bat把其中的服务器IP地址修改为你的实际服务器IP</p>
<p>至于kcptun_client.json参数建议默认，不要改动。</p>
<p>3、配置SS客户端</p>
<p>SS客户端不适用KCPTUN和udp2raw的时候应该是可以正常使用的，我们前面说过，这时候我们只需要修改SS的客户端配置，将服务器IP改为127.0.0.1，将端口改为8888即可（这里对应kcptun_client.json中监听的端口，如果你没有改动直接使用8888即可），其他不需要改变。</p>
<p>这时候启动文件夹的start.bat，SS客户端正常开启，测试是否OK。</p>
<p>说明：kcptun的参数每个人的情况可能不同，你可以适当的调整，实际测试速度，找到最适合你的参数。</p>
]]></content>
    </entry>
</feed>